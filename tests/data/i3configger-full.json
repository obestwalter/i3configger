{
  "settings": {
    "marker": "not marker default",
    "template": "not template default"
  },
  "defaults": {
    "font": "some default font",
    "selector": "some default source"
  },
  "bars": {
    "first bar": {
      "font": "should override font default",
      "output": "first output"
    },
    "second bar": {
      "output": "second output",
      "selector": "should override source default"
    }
  }
}
